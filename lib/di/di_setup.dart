import 'package:get_it/get_it.dart';

final getIt = GetIt.instance;

void diSetup() {
  // registerSingleton
  // getIt.registerSingleton<Repository>(RepositoryImpl());

  // registerFactory
  // getIt.registerFactory<ViewModel>(() => ViewModel());
}